#!/usr/bin/env bash

function bootstrap {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  pkg ensure lang/ruby22-base
  pkg ensure lang/ruby22-gdbm
  pkg ensure lang/ruby22-readline
  gem install bundler
}

bootstrap
